title: "Training"
baseurl: ""  # or "/training" only for project pages

markdown: kramdown

collections:
  training:
    output: true
    permalink: /training/:name/

defaults:
  - scope:
      path: ""
      type: training
    values:
      layout: default
