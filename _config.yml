title: "Training"
baseurl: "" # change to "/training" only if using project pages

markdown: kramdown

collections:
  training:
    output: true
    permalink: /training/:name/

defaults:
  - scope:
      path: ""
      type: training
    values:
      layout: default
