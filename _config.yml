title: "Training"
baseurl: "/training"  # required for GitHub project pages
url: "https://advisorpages.github.io"  # replace with your GitHub username

markdown: kramdown

collections:
  training:
    output: true
    permalink: /:name/  # clean URL: /training/slug/

defaults:
  - scope:
      path: ""
      type: training
    values:
      layout: default
